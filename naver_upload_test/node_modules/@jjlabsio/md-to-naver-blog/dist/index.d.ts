interface UrlContext {
    type: "link" | "image";
    raw: string;
}
interface ComponentProps {
    [key: string]: string;
}
type ComponentRenderer = (props: ComponentProps, children: string) => string;
interface ConvertOptions {
    transformUrl?: (ctx: UrlContext) => string;
    components?: Record<string, ComponentRenderer>;
}
interface RenderedBlock {
    id: string;
    type: string;
    html: string;
}
type RenderCache = Map<string, {
    type: string;
    html: string;
}>;
interface ConvertResult {
    title: string;
    html: string;
    frontmatter: Record<string, unknown>;
    blocks: RenderedBlock[];
}
declare function convert(markdown: string, options?: ConvertOptions, cache?: RenderCache): ConvertResult;
declare function toNaverPasteHtml(html: string): string;
declare function getHtmlClipboardScript(html: string): string;
declare function getTextClipboardScript(text: string): string;

export { type ComponentProps, type ComponentRenderer, type ConvertOptions, type ConvertResult, type RenderCache, type RenderedBlock, type UrlContext, convert, getHtmlClipboardScript, getTextClipboardScript, toNaverPasteHtml };
